{
  "name": "agami-telemetry-endpoint",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker that receives anonymous usage events from the agami OSS skill.",
  "main": "src/worker.ts",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260501.0",
    "typescript": "^5.4.0",
    "vitest": "^1.5.0",
    "wrangler": "^3.50.0"
  }
}
